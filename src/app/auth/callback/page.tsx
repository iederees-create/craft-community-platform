"use client";
import { useEffect,useState } from "react";import { useRouter,useSearchParams } from "next/navigation";import { Suspense } from "react";import { getSupabaseBrowserClient } from "@/lib/supabase/client";
function Callback(){const query=useSearchParams();const router=useRouter();const code=query.get("code");const [message,setMessage]=useState(code?"Completing authentication…":"Authentication code is missing.");useEffect(()=>{if(!code)return;getSupabaseBrowserClient().auth.exchangeCodeForSession(code).then(({error})=>{if(error)setMessage(error.message);else router.replace(query.get("next")||"/onboarding")})},[code,query,router]);return <p role="status">{message}</p>};export default function Page(){return <main className="mx-auto max-w-xl p-8"><h1 className="mb-4 text-3xl font-semibold">Authentication callback</h1><Suspense fallback="Completing authentication…"><Callback/></Suspense></main>}
